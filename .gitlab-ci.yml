image: "rust:latest"

before_script:
  - apt-get update && apt-get install -y extra-runtime-dependencies && rm -rf /var/lib/apt/lists/*
  - rustc --version && cargo --version

test:cargo:
  script:
    - cargo test --workspace --verbose

run:cargo:
    script:
    - cargo build -r
